"ssq-ui": minor
---

Add a virtualized `Table` component with sorting, filtering, fixed columns, sticky headers, and keyboard navigation.

Introduce a reusable `Tooltip`, unify overlay accessibility behavior across floating components, and expand runtime theming with token overrides plus an interactive theme editor.

Improve the delivery pipeline with coverage reporting, GitHub Pages preview workflows, and automated changesets-based release management.
